{
  "$comment": [
    "MongoDB Search (full-text) index for the `documents` collection.",
    "",
    "Serves browse substring search: the `search` parameter on `list_sources`,",
    "`/api/sources`, `/api/documents` and the `/documents` page. The custom",
    "keywordLowercase analyzer indexes each field's whole value as a single",
    "lowercased term, so a `wildcard` query delivers the case-insensitive",
    "substring semantics the API promises while staying on the index — a",
    "case-insensitive $regex can never use one. The `token` mapping on `title`",
    "and the date/number fields exist for the $search `sort` option.",
    "Applied as code by `npm run db:indexes` — see chunks.vector.json.",
    "",
    "`domainPath` is the materialised ancestor chain of `domain` - token-equals",
    "against it gives the browse-search domain filter prefix semantics, exactly",
    "like chunks.text.json. Normalizer `none`, not `lowercase`: `domain` is",
    "documented as case-sensitive."
  ],
  "analyzers": [
    {
      "name": "keywordLowercase",
      "tokenizer": { "type": "keyword" },
      "tokenFilters": [{ "type": "lowercase" }]
    }
  ],
  "mappings": {
    "dynamic": false,
    "fields": {
      "title": [
        { "type": "string", "analyzer": "keywordLowercase" },
        { "type": "token", "normalizer": "lowercase" }
      ],
      "sourceId": [{ "type": "string", "analyzer": "keywordLowercase" }],
      "uri": [{ "type": "string", "analyzer": "keywordLowercase" }],
      "tags": [{ "type": "token", "normalizer": "lowercase" }],
      "domainPath": [{ "type": "token", "normalizer": "none" }],
      "createdAt": [{ "type": "date" }],
      "updatedAt": [{ "type": "date" }],
      "chunking": {
        "type": "document",
        "fields": {
          "chunkCount": [{ "type": "number" }]
        }
      }
    }
  }
}
