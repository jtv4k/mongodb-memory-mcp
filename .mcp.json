{
  "mcpServers": {
    "mongodb-memory-mcp": {
      "type": "http",
      "url": "http://${MCP_HOST:-localhost}:${MCP_PORT:-3000}${MCP_PATH:-/mcp}",
      "headers": {
        "Authorization": "Bearer ${MCP_MONGODB_MEMORY_TOKEN}"
      }
    }
  }
}
